# What the HTTP front end needs in Lambda, which is requirements.txt minus the
# two things only a laptop uses.
#
#   psycopg   the local Postgres in docker-compose.yml. Nothing the service
#             exposes over HTTP reads it -- saved stocks and tracked contracts
#             are still script-only -- so the driver would be 15MB of image
#             that never gets imported.
#   uvicorn   the server. Lambda is the server here; Mangum translates its
#             event into the ASGI call uvicorn would have made.
#
# Versions are left as the floors requirements.txt sets rather than pinned, so
# this file cannot quietly disagree with the one the tests run against.

yfinance>=0.2.40
pandas>=1.3
numpy>=1.21
lxml>=4.9
requests>=2.31
fastapi>=0.110

# The ASGI adapter. Not in requirements.txt because it is meaningless off
# Lambda -- nothing else speaks API Gateway's event shape.
mangum>=0.17
